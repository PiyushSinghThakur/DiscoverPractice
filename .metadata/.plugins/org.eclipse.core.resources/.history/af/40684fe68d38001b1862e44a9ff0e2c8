package com.cg.associate.dao;

import java.util.List;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.query.Param;
import org.springframework.stereotype.Repository;

import com.cg.associate.entity.Employee;

@Repository
public interface EmployeeRepository extends JpaRepository<Employee, Long>{

	//@Query(value = "select e from Employee e where empAge=:age")
	//List<Employee> findByAge(@Param("age")int empAge);
	
	//@Query(value = "select e from Employee e where empAge=:age and countryCode=:code")
	//List<Employee> findByAgeAndCode(@Param("age")int empAge, @Param("code")int countryCode);
	//List<Employee> findByAge(int empAge);
	//List<Employee> findByAgeAndCode(int empAge, int countryCode);

	List<Employee> findByEmpAge(int empAge);

	List<Employee> findByEmpAgeAndCode(int empAge, int countryCode);
}
