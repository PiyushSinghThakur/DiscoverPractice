package com.cg.associate;

import java.util.HashSet;
import java.util.Set;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

import com.cg.associate.entity.Address;
import com.cg.associate.entity.Employee;

@SpringBootApplication
public class AssociationApplication {

	public static void main(String[] args) {
		SpringApplication.run(AssociationApplication.class, args);
		Address address1= new Address(1, "noida", "india");
		Address address2= new Address(2, "pune", "india");
		Address address3= new Address(3, "bhopal", "india");
		Employee emp1 = new Employee(1, "piyush", 22, 91, address1);
		Employee emp2 = new Employee(3, "pradeep", 24, 91, address3);
		Employee manager = new Employee(2,"swadeep",30,91,address2);
		//Set<Employee> subordinates = new HashSet<>();
		
	}

}